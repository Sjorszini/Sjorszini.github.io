(function(){
  "use strict";
  var worker=null, liveTimer=null, liveStart=0, liveLabel="", runToken=0;
  function el(id){ return document.getElementById(id); }
  function fmt(ms){ if(ms<1000) return ms.toFixed(0)+" ms"; if(ms<60000) return (ms/1000).toFixed(ms<10000?2:1)+" s"; return Math.floor(ms/60000)+"m "+((ms%60000)/1000).toFixed(1)+"s"; }
  function escapeHtml(s){ return String(s).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;"); }
  function tex(expr){ try { return math.parse(expr).toTex({parenthesis:"keep"}).replace(/\bx(\d+)\b/g,"x_{$1}").replace(/\by(\d+)\b/g,"y_{$1}"); } catch(e){ return "\\text{"+escapeHtml(expr).replace(/[{}]/g,"")+"}"; } }
  function typeset(node){ if(window.MathJax&&window.MathJax.typesetPromise) return window.MathJax.typesetPromise([node]); return Promise.resolve(); }
  function setStatus(text,type){ var s=el("status"); if(!s) return; s.textContent=text; s.className="calc-status"+(type?" is-"+type:""); }
  function setBusy(on){ ["computeConnection","computeCurvature","loadExample"].forEach(function(id){ var n=el(id); if(n) n.disabled=on; }); }
  function stopLive(){ if(liveTimer){ clearInterval(liveTimer); liveTimer=null; } }
  function startLive(label){ stopLive(); liveLabel=label; liveStart=performance.now(); updateLive(); liveTimer=setInterval(updateLive,100); }
  function updateLive(){ var n=el("live-step"); if(n) n.textContent=liveLabel+" — "+fmt(performance.now()-liveStart)+" elapsed"; }
  function prepareResults(){ var r=el("results"); r.innerHTML='<section class="result-card calculation-progress" id="calculation-progress"><div class="result-card-header"><h2>Calculation progress</h2><span class="result-meta" id="path-label">starting…</span></div><p id="live-step" class="live-step">Preparing calculation…</p><div id="timing-log" class="timing-log"></div></section><div id="progressive-sections" class="results-stack"></div>'; return r; }
  function ensureSection(key,title,meta){ var id="section-"+key, n=el(id); if(n) return n; n=document.createElement("section"); n.id=id; n.className="result-card progressive-result"; n.innerHTML='<div class="result-card-header"><h2>'+escapeHtml(title)+'</h2><span class="result-meta" data-time>working…</span></div><p class="result-note">'+escapeHtml(meta||"")+'</p><div class="component-list" data-components></div><div class="section-summary" data-summary></div>'; el("progressive-sections").appendChild(n); return n; }
  function logTiming(text){ var log=el("timing-log"); if(!log) return; var p=document.createElement("div"); p.className="timing-log-row"; p.textContent=text; log.appendChild(p); }
  function appendComponent(msg){ var sec=el("section-"+msg.section); if(!sec) return; var list=sec.querySelector("[data-components]"); var row=document.createElement("div"); row.className="component-row timed-component"; row.innerHTML='<div class="component-formula">\\['+msg.label+'='+tex(msg.value)+'\\]</div><span class="component-time">'+fmt(msg.elapsedMs)+'</span>'; list.appendChild(row); typeset(row); }
  function renderSummary(sec,summary){ if(!summary) return; var box=sec.querySelector("[data-summary]"); if(summary.matrix){ var rows=summary.matrix.map(function(row){ return row.map(tex).join(" & "); }); box.innerHTML+='<div class="math-block">\\[\\begin{pmatrix}'+rows.join(" \\\\ ")+'\\end{pmatrix}\\]</div>'; } if(summary.det){ box.innerHTML+='<div class="math-block">\\[\\det(g)='+tex(summary.det)+'\\]</div>'; } if(summary.homogeneous!==undefined){ box.innerHTML+='<p class="result-note">2-homogeneity: <strong>'+(summary.homogeneous?'verified symbolically':'residual did not simplify to zero')+'</strong>.</p>'; } if(summary.riemannian!==undefined){ box.innerHTML+='<p class="result-note">Detected geometry: <strong>'+(summary.riemannian?'quadratic / Riemannian':(summary.randers?'2D Randers':'general Finsler'))+'</strong>.</p>'; } typeset(box); }
  function handleMessage(e,token){ if(token!==runToken) return; var m=e.data||{}; if(m.type==="path"){ var p=el("path-label"); if(p) p.textContent=m.path; return; } if(m.type==="stepStart"){ startLive(m.label); setStatus(m.label+"…","working"); return; } if(m.type==="sectionStart"){ ensureSection(m.section,m.title,m.meta); return; } if(m.type==="component"){ appendComponent(m); logTiming(m.label+" — "+fmt(m.elapsedMs)); return; } if(m.type==="sectionComplete"){ var sec=el("section-"+m.section); if(sec){ var t=sec.querySelector("[data-time]"); if(t) t.textContent="completed in "+fmt(m.elapsedMs); renderSummary(sec,m.summary); } logTiming((sec&&sec.querySelector("h2")?sec.querySelector("h2").textContent:m.section)+" completed — "+fmt(m.elapsedMs)); return; } if(m.type==="done"){ stopLive(); var live=el("live-step"); if(live) live.textContent="Calculation complete — total "+fmt(m.totalMs); setStatus("Calculation complete in "+fmt(m.totalMs)+".","success"); setBusy(false); return; } if(m.type==="error"){ stopLive(); setStatus(m.message||"Calculation failed.","error"); var live2=el("live-step"); if(live2) live2.textContent="Stopped: "+(m.message||"error"); setBusy(false); return; } }
  function run(mode,event){ if(event){ event.preventDefault(); event.stopImmediatePropagation(); } var n=Number(el("dimension").value), L=el("lagrangian").value.trim(); if(!L){ setStatus("Enter a Finsler Lagrangian first.","error"); return; } try{ math.parse(L); }catch(error){ setStatus(error.message||String(error),"error"); return; } if(worker) worker.terminate(); worker=new Worker("finsler-progressive-worker.js?v=1"); runToken+=1; var token=runToken; prepareResults(); setBusy(true); setStatus("Starting symbolic calculation…","working"); startLive("Starting worker"); worker.onmessage=function(e){ handleMessage(e,token); }; worker.onerror=function(e){ if(token!==runToken) return; stopLive(); setBusy(false); setStatus("Worker error: "+(e.message||"unknown error"),"error"); }; worker.postMessage({type:"calculate",mode:mode,n:n,L:L}); }
  function loadExample(){ var v=el("exampleSelect").value; if(v==="randers"){ el("dimension").value="2"; el("lagrangian").value="(sqrt(exp(2*x1)*y1^2 + y2^2) + 0.18*y2)^2"; } else if(v==="riemann2"){ el("dimension").value="2"; el("lagrangian").value="(1 + x1^2)*y1^2 + exp(2*x1)*y2^2"; } else if(v==="minkowski4"){ el("dimension").value="4"; el("lagrangian").value="-y1^2 + y2^2 + y3^2 + y4^2"; } if(worker){ worker.terminate(); worker=null; } stopLive(); el("results").innerHTML=""; setBusy(false); setStatus("Example loaded.",""); }
  function clear(){ if(worker){ worker.terminate(); worker=null; } runToken+=1; stopLive(); el("results").innerHTML=""; setBusy(false); setStatus("Ready.",""); }
  function init(){ el("computeConnection").addEventListener("click",function(e){run("connection",e);},true); el("computeCurvature").addEventListener("click",function(e){run("curvature",e);},true); el("loadExample").addEventListener("click",function(e){e.preventDefault();e.stopImmediatePropagation();loadExample();},true); el("clearResults").addEventListener("click",function(e){e.preventDefault();e.stopImmediatePropagation();clear();},true); }
  if(document.readyState==="loading") document.addEventListener("DOMContentLoaded",init); else init();
})();
